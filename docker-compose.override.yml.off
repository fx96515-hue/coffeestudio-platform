services:
  frontend:
    environment:
      NEXT_PUBLIC_API_BASE_URL: "http://localhost:8000"
      API_BASE_URL: "http://localhost:8000"
  backend:
    environment:
      CORS_ORIGINS: "http://localhost:3000,http://ui.localhost,http://localhost,http://127.0.0.1:3000,http://127.0.0.1"
